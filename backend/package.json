{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "Backend API for Dhiraj Pandit Portfolio",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "setup:admin": "node scripts/setup-admin.js",
    "seed": "node scripts/seed-data.js",
    "seed:skills": "node scripts/seed-skills.js",
    "setup": "npm run db:generate && npm run db:push && npm run setup:admin && npm run seed && npm run seed:skills"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "bcryptjs": "^2.4.3",
    "cloudinary": "^2.8.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.20.0",
    "multer": "^2.0.2",
    "nodemailer": "^6.9.7",
    "prisma": "^5.7.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
